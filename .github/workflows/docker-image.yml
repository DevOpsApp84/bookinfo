name: Docker Image CI Build
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ webapp ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        ls ${{ github.workspace }}
